type Props = {
  title?: string;
  description?: string;
  canonical?: string;
};

export default function SEO(_props: Props) {
  // Next.js handles SEO via generateMetadata in route files
  return null;
}
